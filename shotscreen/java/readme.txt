1.sql文件注释补全
  我在sql脚本我文件中注释要求创建一个role表，smartcode给我了正确的提示。
2.sql上下补全
  随后回车，smartcode提供了insert脚本，字段由role的建表脚本推断出来，正确
3.java上下文提示
  我准备手动创建role实体，在我声明了实体名为role后，smartcode给了我实体完成的声明方案
3.java注释代码补全
  随后创建了role实体，并在编写role的仓储脚本时告诉smartcode我想要一个jdbc的仓储层，smartcode按照我的需求提供了引用的包
4.java跨文件补全（读取sql）
  如图所示，不但提供了java相关的提示方案，在执行数据操作时给出了正确的脚本方案。

shell
